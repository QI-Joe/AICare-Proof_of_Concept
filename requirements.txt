# Core ML and NLP libraries
torch
torchvision
transformers>=4.35.0
numpy>=1.21.0
torchcodec

# Message queue and networking
pika>=1.3.0
websockets>=11.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
PyAudio

# Dataset and data processing
datasets>=2.14.0

# Audio processing
pyaudio>=0.2.11

# Standard libraries (usually included with Python, but listed for completeness)
# sudo apt install portaudio19-dev / erlang / rabbitmq-server required
# brew install ffmpeg 
# https://stackoverflow.com/questions/73268630/error-could-not-build-wheels-for-pyaudio-which-is-required-to-install-pyprojec 
# for audio problem

# asyncio - built-in
# json - built-in
# base64 - built-in
# time - built-in
# os - built-in
# re - built-in
# typing - built-in (Python 3.5+)
# uuid - built-in
# threading - built-in
# logging - built-in